import React from 'react';
import logo from '../assets/header.jpeg';

const Header = () => {
  return (
    <header>
      <img src={logo} alt='poloniny' />
    </header>
  );
};

export default Header;
